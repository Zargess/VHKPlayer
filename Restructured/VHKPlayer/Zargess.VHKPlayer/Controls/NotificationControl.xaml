<UserControl x:Class="Zargess.VHKPlayer.Controls.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:Zargess.VHKPlayer.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="Control">
    <Grid>
        <Grid x:Name="Ild">
            <!--<i:Interaction.Triggers>
                <i:EventTrigger SourceObject="Ild" EventName="PreviewMouseLeftButtonDown">
                    <i:InvokeCommandAction Command="{Binding ClickedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <TextBlock />-->
        </Grid>
        <Popup x:Name="pop" IsOpen="True" PlacementTarget="{Binding ElementName=Ild}">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Popup>
    </Grid>
</UserControl>
